#!/usr/bin/node
/* Computes and prints factorial */

function recursion_factorial (a) {
  const num = parseInt(a);
  if (num === 0 || isNaN(num)) {
    return 1;
  }
  return num * recursion_factorial(num - 1);
}

console.log(recursion_factorial(process.argv[2]));
